<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             x:Class="LiteMigrator.Sample.Client.Views.MainView"
             Title="{Binding Title}"
             mc:Ignorable="d">

  <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
    <Label FontSize="Large" Text="Welcome to LiteMigrator Sample!" />

    <Button Command="{Binding CmdGetCurrentVersion}" Text="Get Current Migration Version" />
    <Button Command="{Binding CmdMigrateUp}" Text="Apply Latest Migration(s)" />

    <StackLayout Orientation="Horizontal">
      <Label FontAttributes="Bold" Text="Version:" />
      <Label Text="{Binding LatestVersion}" />
    </StackLayout>

    <Label FontAttributes="Italic" Text="{Binding ErrorMessage}" />

  </StackLayout>
</ContentPage>